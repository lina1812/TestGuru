<div class="page-header">
  <h1><%= t(".header") %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= t(Gist.human_attribute_name(:question)) %></th>
      <th><%= t(Gist.human_attribute_name(:url)) %></th>
      <th><%= t(Gist.human_attribute_name(:user)) %></th>
    </tr>
  </thead>
  <tbody>
    <%= render @gists %>
  </tbody>
</table>