<h3><%= t('.header') %></h3>
<table>
  <thead>
    <tr>
      <th><%= t(Question.human_attribute_name(:body)) %></th>
      <th colspan="3"><%= t('.actions') %></th>
    </tr>
  </thead>
  <tbody>
    <%= render partial: 'admin/questions/question', collection: @questions %>
  </tbody> 
</table>
<p>
  <%= link_to t('.new'), new_admin_test_question_path(@test, @question) %>
</p>
